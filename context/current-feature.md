# Current Feature

## Status

Not Started

## Goals

## Notes

## History

- 2026-04-09: Initial Next.js 16 + Tailwind v4 setup, removed boilerplate assets, added project context files
- 2026-04-09: Dashboard Phase 1 — shadcn/ui init, dark mode, top bar, dashboard layout with sidebar/main placeholders
- 2026-04-09: Dashboard Phase 2 — Collapsible sidebar with item types, favorite/recent collections, user avatar, and mobile drawer
- 2026-04-09: Dashboard Phase 3 — Stats cards, collections grid, pinned items, recent items in main content area
- 2026-04-10: Dashboard Collections — Replaced dummy data with real Prisma queries, created src/lib/db/collections.ts, dominant type border color, type icons
- 2026-04-10: Dashboard Items — Replaced dummy item data with real Prisma queries for pinned items, recent items, and stats cards
- 2026-04-10: Stats & Sidebar — Real data for sidebar item types with counts, favorite/recent collections with dominant color circles, "View all collections" link, seed updated with favorite collections
- 2026-04-11: Pro Badge — Added PRO badge (shadcn/ui Badge) to File and Image types in sidebar
- 2026-04-11: Quick Wins — Deleted dead mock-data.ts, extracted shared iconMap and formatDate, fixed N+1 in getSidebarCollections, added take limits, replaced raw button with shadcn Link+buttonVariants
- 2026-04-11: Auth Phase 1 — NextAuth v5 with GitHub OAuth, split config pattern, Prisma adapter with JWT, /dashboard/* route protection via proxy, Session type extended with user.id
- 2026-04-11: Auth Phase 2 — Credentials provider with bcrypt validation, POST /api/auth/register endpoint with input validation and duplicate checking
- 2026-04-11: Auth Phase 3 — Custom sign-in/register pages, reusable UserAvatar component, sidebar user menu with sign-out dropdown, dashboard uses real auth session, toast on registration
- 2026-04-12: Email Verification — Resend integration, EmailVerificationToken model, verify/resend API routes, verify-email page, proxy blocks unverified users from dashboard, delete-users utility script
- 2026-04-12: Email Verification Toggle — Added EMAIL_VERIFICATION_ENABLED env flag to enable/disable email verification, defaults to false for development without a Resend domain
- 2026-04-12: Forgot Password — Forgot/reset password flow using existing VerificationToken model, Resend emails, forgot-password and reset-password pages, "Forgot password?" link on sign-in page
- 2026-04-12: Profile Page — /profile route with user info (avatar, email, name, join date), usage stats (total items/collections, breakdown by item type), change password form (credentials users only), delete account with confirmation dialog, proxy updated to protect /profile
- 2026-04-12: Rate Limiting — Upstash Redis rate limiting on all auth endpoints (login, register, forgot/reset password, resend verification), reusable rate-limit utility with fail-open, frontend 429 error handling
- 2026-04-12: Fix GitHub OAuth Redirect — Switched GitHub sign-in from client-side signIn to server action with redirectTo, fixing the double-click redirect issue
- 2026-04-13: Items List View — Dynamic /items/[type] route with type-filtered Prisma query, reusable ItemCard with left border by type color, responsive 2-col grid on md+, shared dashboard shell layout, proxy protects /items/*
- 2026-04-13: Vitest Setup — Vitest configured for node env with native tsconfig path resolution, scoped to src/{lib,actions}/**/*.test.ts, sample test for formatDate, test/test:watch/test:coverage scripts, workflow docs updated to require npm test
- 2026-04-13: Items List 3-Column Grid — /items/[type] grid bumped to lg:grid-cols-3 while keeping 1-col mobile and 2-col md
- 2026-04-13: Item Drawer — Right-side shadcn Sheet drawer with action bar (favorite/pin/copy/edit/delete), opens on ItemCard/PinnedItems/RecentItems click on dashboard and items list. New getItemDetail query, /api/items/[id] auth-checked route, ItemDrawerProvider client context, skeleton while fetching
- 2026-04-15: Item Drawer Edit Mode — Inline edit toggle in drawer with Save/Cancel bar, controlled form (title/description/tags + type-specific content/language/url), new updateItem server action with Zod validation and ownership check, updateItem query with tag disconnect+connectOrCreate, shadcn Textarea component, zod dep, router.refresh() on save, Vitest coverage for the action
- 2026-04-15: Item Delete — New deleteItem server action (auth + ownership via deleteMany) and deleteItem query, drawer delete button now opens a shadcn Dialog confirmation, success/error toast, drawer closes and router.refresh() on success, Vitest coverage for unauthorized/not-found/success/throw
- 2026-04-15: Item Create — New createItem query (resolves system ItemType by name, connectOrCreate tags, sets contentType text/url) and createItem server action with Zod validation (enum type, title required, link requires URL via superRefine), new NewItemDialog client component wired into TopBar with type selector and dynamic content/language/url fields, toast + close + form reset + router.refresh() on success, Vitest coverage for unauthorized/empty title/link without url/success
- 2026-04-16: Code Editor — Monaco Editor component (CodeEditor.tsx) with macOS window dots, copy button, language label, vs-dark theme, fluid height (max 400px). Replaces <pre><code> display and Textarea editing for snippet/command types in ItemDrawer and NewItemDialog. Notes/prompts keep Textarea.
- 2026-04-16: Markdown Editor — MarkdownEditor component (MarkdownEditor.tsx) with Write/Preview tabs, react-markdown + remark-gfm, dark theme .markdown-preview CSS (headings, code blocks, lists, blockquotes, links, tables). Replaces Textarea for note/prompt types in ItemDrawer (view + edit) and NewItemDialog.
- 2026-04-19: Remove File/Image Types — Dropped File and Image system types, removed fileUrl/fileName/fileSize columns from Item (prisma migration), trimmed seed, iconMap, TYPE_LABELS, sidebar PRO gating, and ItemDrawer file viewer. Updated project-overview, item-types, item-crud-architecture docs to reflect the 5-type system (snippet, prompt, command, note, link).
- 2026-04-20: Collection Create — New createCollection query in src/lib/db/collections.ts (user-scoped via user.connect, returns CollectionSummary) and createCollection server action with Zod validation (name required, description trimmed/nullable), new NewCollectionDialog client component replacing TopBar placeholder button, toast + close + form reset + router.refresh() on success, Vitest coverage for unauthorized/empty name/success/description trim/query throw.
- 2026-04-20: Item → Collections Assignment — New getUserCollections / getUserCollectionIds queries (user-scoped, alphabetical), auth-checked GET /api/collections route, CollectionMultiSelect client component (fetches on mount, Badge-chip toggle UI) wired into NewItemDialog and ItemDrawer edit form with pre-population from item.collections. createItem/updateItem actions + queries accept collectionIds with ownership validation (foreign ids silently dropped); createItem uses nested ItemCollection writes, updateItem reconciles via $transaction (deleteMany + createMany). Vitest coverage for default-empty, owned pass-through, and foreign-id filtering on both actions.
- 2026-04-20: Collections Pages — New /collections index (getAllCollectionsForUser, favorites first then updatedAt desc) and /collections/[id] detail (getCollectionDetail, user-scoped with notFound on miss) using shared dashboard shell layout. Extracted CollectionCard component from CollectionsSection, wrapped in Link to /collections/[id], reused on dashboard. Items in collection detail rendered via existing ItemCard in 1/2/3-col responsive grid. Proxy matcher + protected prefix extended to /collections/*.
- 2026-04-20: Collection Edit/Delete/Favorite Actions — New updateCollection + deleteCollection queries and server actions (Zod + ownership checks). Reusable EditCollectionDialog + DeleteCollectionDialog. CollectionCardMenu adds a 3-dots dropdown (Edit/Favorite/Delete) to cards with preventDefault/stopPropagation so the card Link still navigates on non-trigger clicks. CollectionDetailActions puts Favorite/Edit/Delete icon buttons on /collections/[id]; delete redirects to /collections. Items are preserved on collection delete (Prisma cascades ItemCollection only). Favorite is a placeholder toast. Vitest coverage for both new actions.
- 2026-04-20: Global Search / Command Palette — Cmd+K/Ctrl+K opens a shadcn cmdk palette with fuzzy client-side search. New getSearchData query (items + collections, user-scoped) and auth-checked /api/search route. CommandPaletteProvider prefetches on mount and open, registers the global hotkey; CommandPalette groups results (items show type icon + preview, collections show item count), selects open the item drawer or navigate to /collections/[id]. SearchTrigger replaces the TopBar input with a button showing the ⌘K/Ctrl+K hint. Fixed shadcn CommandDialog to wrap children in <Command> so CommandInput has cmdk context. Vitest coverage for getSearchData preview precedence and truncation.
- 2026-04-20: Pagination — New src/lib/pagination.ts with ITEMS_PER_PAGE/COLLECTIONS_PER_PAGE (21) + DASHBOARD_COLLECTIONS_LIMIT (6) / DASHBOARD_RECENT_ITEMS_LIMIT (10) and parsePageParam/clampPage/getPageCount helpers. Replaced getItemsByType with getItemsByTypePage(skip, take) → {items, total} and getCollectionDetail with getCollectionDetailPage (paginates ItemCollection with parallel count). New server-safe shadcn-style Pagination component with windowed page numbers and greyed-out prev/next at bounds. /items/[type] and /collections/[id] now read ?page=, fetch only the slice, clamp out-of-range. Dashboard queries (getRecentItems, getCollectionsForDashboard) bound by the new constants. Vitest coverage for pagination helpers and both paginated queries.
- 2026-04-21: Settings Page — New protected /settings route (proxy prefix + matcher extended) that hosts the Account actions previously on /profile: change-password form (credentials users only) and delete-account danger zone, reusing ChangePasswordForm and DeleteAccountButton unchanged. Profile page trimmed to identity + usage stats only. Sidebar user dropdown (collapsed and expanded) gains a "Settings" link alongside Profile/Sign out, with matching lucide icons.
- 2026-04-21: Editor Preferences — New User.editorPreferences JSON column (migration 20260421105822_add_editor_preferences), src/lib/editor-preferences.ts with defaults + Zod schema + normalize helper, getEditorPreferences/updateEditorPreferences db helpers, auth-checked updateEditorPreferences server action, EditorPreferencesProvider (optimistic update + rollback + success toast) fed by a server EditorPreferencesLoader mounted in the dashboard/items/collections layouts and settings page. New Editor Preferences card on /settings with auto-save font size, tab size, theme selects and word wrap / minimap switches (shadcn-style Select + Switch built on base-ui). CodeEditor consumes context for fontSize/tabSize/wordWrap/minimap/theme; custom monokai and github-dark themes registered via beforeMount and title bar background matches the active theme. Vitest coverage for normalizeEditorPreferences and the server action (auth, invalid font size, unknown theme, success, default payload, query throw).
- 2026-04-21: Favorites Page — New protected /favorites route (proxy prefix + matcher extended) showing a compact, monospace list of user-favorited items and collections in separate sections with counts. New src/lib/db/favorites.ts getFavorites query (user-scoped, both sets sorted by updatedAt desc, collection itemCount via _count). FavoriteItemRow opens the existing ItemDrawer on click; FavoriteCollectionRow links to /collections/[id]. Star icon button in TopBar links to /favorites. Empty state when no favorites. Vitest coverage for scoping, shape mapping, and empty case.
- 2026-04-21: Favorite Toggle — New toggleItemFavorite / toggleCollectionFavorite db helpers (ownership-scoped findFirst + update) and matching server actions returning { id, isFavorite }. Wired real handlers into the ItemDrawer star button (previously disabled), CollectionDetailActions and CollectionCardMenu (previously placeholder toasts), and added a hover-revealed star button on ItemCard that stays visible when favorited. All client callers use optimistic local state with rollback-on-failure toast and router.refresh() so sidebar favorites, /favorites, and dashboard stay in sync. CollectionCardMenu now takes isFavorite and swaps label between Favorite/Unfavorite. Vitest coverage for both new actions (unauthorized, not-found, toggle on/off, query throw).
- 2026-04-21: Favorites Page Client-Side Sorting — Extracted favorites list rendering into a new client component FavoritesList.tsx that holds sort state (date/name/type) and re-sorts items + collections with useMemo. shadcn-style Select in the top-right with ArrowUpDown icon + "Sort" label, compact h-8 trigger. Items sort by itemType.name then title in type mode; collections fall back to name in type mode (no type to sort by). Server page.tsx still handles auth, getFavorites, header and empty state.
- 2026-04-24: Pinned Items — New toggleItemPin db helper (ownership-scoped findFirst + update) and server action returning { id, isPinned }. Wired the previously-disabled Pin button in ItemDrawer with optimistic local state, rollback-on-failure toast, success toast ("Item pinned"/"Item unpinned"), and router.refresh(). getItemsByTypePage and getCollectionDetailPage now sort [isPinned desc, updatedAt desc] so pinned items bubble to the top of listings. ItemCard shows a static Pin icon next to the title when isPinned. Updated existing sort-order test assertions. Vitest coverage for toggleItemPin (unauthorized / not-found / toggle on / toggle off / query throw). Also added nativeButton={false} to the TopBar favorites Button to silence a pre-existing Base UI warning on the Link-rendered button.
- 2026-04-24: Homepage Mockup — New standalone marketing prototype at prototypes/homepage/ (index.html, styles.css, script.js), not wired into the Next.js app. Hero with "chaos → order" visual: 8 floating tool glyphs (Notion/GitHub/Slack/VS Code/tabs/terminal/file/bookmark as simplified inline SVGs) animated via requestAnimationFrame (wall bounce, mouse repel, sine-scale pulse, rotation) with ResizeObserver-bounded stage, pulsing gradient arrow, dashboard preview mockup. Fixed navbar with opacity-on-scroll, 6-card features grid using item-type accent palette (Snippet #3b82f6, Prompt #f59e0b, Command #06b6d4, Note #22c55e, File #64748b, Image #ec4899, URL #6366f1 — spec palette, not the in-app one), AI section with Pro badge + checklist + editor mockup + AI-generated tags, pricing monthly/yearly toggle ($8/mo ↔ $6/mo $72 billed yearly) with "Most popular" on Pro, CTA band and footer with dynamic year. IntersectionObserver scroll reveal; prefers-reduced-motion collapses animations and lays chaos icons out in a static grid.
- 2026-04-24: Homepage — Ported prototypes/homepage into the real app at /. New src/app/page.tsx server component runs auth() and redirects signed-in users to /dashboard, otherwise renders the marketing page with `metadata` export (title + description from the prototype head). New src/components/home/ module with barrel index.ts: server components (Logo, Navbar, Hero, HeroVisual, DashboardMock, Features, FeatureCard, AiSection, CtaBand, Footer) built on Tailwind v4 + shadcn Button/Badge primitives, content (features, AI checklist, plan bullets, footer columns, dashboard mock, AI demo tags) extracted into typed const arrays in data.ts, chaos glyphs as inline SVG JSX in chaos-icons.tsx. Client components: NavbarScroll (sets data-scrolled attr for opaque backdrop), ChaosStage (ported rAF animation — wall bounce, mouse repel, ResizeObserver, reduced-motion static-grid fallback), Pricing (monthly/yearly state, $8/mo ↔ $6/mo + "$72 billed yearly" note). Features grid uses the real 5-type palette (snippet/prompt/command/note/link) + a Collections card; file/image accents dropped. All CTAs wired: Sign in → /sign-in, Get started / Upgrade to Pro / Try Pro free / CTA band → /register, Features/Pricing nav + footer → #features/#pricing anchors, logo → /, blog/legal stay as # placeholders. Item-type accents applied via inline --c CSS var with color-mix for tinted backgrounds. Build + 79 Vitest tests pass.
- 2026-04-24: TopBar Mobile — Dashboard TopBar converted to a client component and made responsive. Brand text hidden below sm (square-only), search field flex-grows (min-w-0 flex-1) and picks up max-w-md + horizontal padding only from sm up, header padding px-3 sm:px-6. The three right-side actions (Favorites, New Collection, New Item) show as separate buttons from md up; below md they collapse into a single MoreHorizontal overflow DropdownMenu with "New item / New collection / Favorites" items. NewItemDialog and NewCollectionDialog refactored to accept optional open/onOpenChange controlled-mode props — when both are provided, their default pill trigger is skipped so TopBar's state can drive them from the mobile menu while still rendering one dialog instance. Uncontrolled usage (no props) behaves exactly as before, so no other callers needed updates. Build + 79 Vitest tests pass. Done directly on main (no feature branch).
- 2026-04-27: Shared Folder Logo + Homepage Link on Auth Pages — Replaced the gradient "D" letter inside the homepage Logo with a Lucide `Folder` icon and extended the component with `href` (defaults to `/`) and `labelClassName` props. Dashboard TopBar dropped its hand-rolled "D" letter-in-a-box markup and now renders the shared `Logo` with `href="/dashboard"` and `labelClassName="hidden sm:inline"`, so homepage and dashboard share the exact same brand mark while the dashboard keeps its mobile-responsive label hide. Added a "← Back to homepage" link below the auth Card on `/sign-in` and `/register` (wrapper switched to `flex-col gap-4` with `py-10`). Also fixed a Tailwind v4 canonical-class warning (`bg-gradient-to-br` → `bg-linear-to-br`). Build + 79 Vitest tests pass.
- 2026-05-06: Stripe Phase 1 — Core Infrastructure — Headless Stripe foundation. New Prisma migration `20260427091721_add_stripe_subscription_state` adds `stripePriceId String?` + `stripeCurrentPeriodEnd DateTime?` to `User`. New `src/lib/stripe.ts` singleton (Stripe SDK, pinned `apiVersion: '2026-04-22.dahlia'`, `STRIPE_PRICE_IDS` map keyed by `BillingInterval`, fail-fast on missing `STRIPE_SECRET_KEY` at import time). New `src/lib/plan-limits.ts` with `FREE_LIMITS = { items: 50, collections: 3 }`, `getUsage(userId)` returning `{ items, collections, isPro }` (parallel counts + isPro lookup, defaults `false` when user missing), and `canCreate(userId, name)` returning `{ allowed, reason?, used, limit }` — Pro users always allowed with `limit: Infinity`. New `src/lib/db/billing.ts` with `getOrCreateStripeCustomerId(userId, email, stripe)` (returns existing without calling Stripe, otherwise creates with `metadata: { userId }` and persists), `setSubscriptionState(customerId, data)` keyed on stripeCustomerId (writes all four billing fields including `isPro`), and `getBillingProfile(userId)` selecting the five billing columns — stripe client passed as arg so only `src/lib/stripe.ts` reads `process.env`. `src/auth.ts` JWT callback re-reads `isPro` from DB on every call (intentional — NextAuth's `trigger === 'update'` unreliable for webhook-driven changes); session callback mirrors onto `session.user.isPro`. `src/types/next-auth.d.ts` adds `Session.user.isPro: boolean` and `JWT.isPro?: boolean`. Vitest coverage: `plan-limits.test.ts` (pro/free × under/at/over for both limits, `getUsage` shape and scoping) and `db/billing.test.ts` (existing customer / new customer / missing user paths, `setSubscriptionState` upgrade + downgrade, `getBillingProfile` field selection). 96 tests pass (was 79), build + lint clean. **Headless only** — no UI, no webhook, no Pro gating on createItem/createCollection; Phase 2 layers those on top. `setSubscriptionState` has no caller this phase, exists for the webhook in Phase 2.
- 2026-05-06: Stripe Phase 2 — Webhook, Gating, and UI — End-to-end billing on top of Phase 1's headless plumbing. New `src/actions/billing.ts` with `createCheckoutSession({ interval })` (Zod, looks up email, calls `getOrCreateStripeCustomerId`, creates a `mode: 'subscription'` Checkout session with `success_url=/settings?checkout=success` / `cancel_url=/settings?checkout=cancel` and `metadata: { userId }` on both session + `subscription_data`) and `createPortalSession()` (returns `'No subscription found'` when `stripeCustomerId` is null, otherwise opens a Billing Portal with `return_url=/settings`). New `src/app/api/stripe/webhook/route.ts` (`runtime = 'nodejs'`, `dynamic = 'force-dynamic'`): rejects missing/invalid `stripe-signature` → 400, verifies via raw `req.text()`, switches on `RELEVANT_EVENTS = { checkout.session.completed, customer.subscription.{created,updated,deleted}, invoice.payment_{succeeded,failed} }` (everything else 200 noop), derives `priceId` from `sub.items.data[0]?.price.id` and `currentPeriodEnd` from `sub.current_period_end * 1000`, sets `isPro = status === 'active' || 'trialing'`, and calls `setSubscriptionState(customerId, …)`. For checkout/invoice events the subscription is fetched via `stripe.subscriptions.retrieve`. Idempotent (keyed on `stripeCustomerId`), throws → 500 so Stripe retries, never logs raw bodies, never calls `auth()`. Free-tier gating: `createItem` and `createCollection` server actions now run `canCreate(session.user.id, 'items'|'collections')` post-Zod and return `'Free plan limit of <N> … reached. Upgrade to Pro for unlimited.'` (skipping the underlying create) when at the cap. New billing UI: `UpgradeButton` (client, calls `createCheckoutSession`, `window.location.href` on success, error toast on failure) and `ManageSubscriptionButton` (client, calls `createPortalSession`); `SubscriptionCard` (server, reads `getBillingProfile`, renders Pro path "DevStash Pro — Renews on {date}" + Manage button or Free path with monthly + yearly upgrade buttons); `CheckoutToast` (client, reads `?checkout=success|cancel` and `router.replace('/settings')`). `/settings` page wires `<CheckoutToast />` (in a Suspense boundary) at the top and `<SubscriptionCard userId={session.user.id} />` between Editor preferences and Account. `NewItemDialog` and `NewCollectionDialog` show an inline amber banner with `<UpgradeButton interval="monthly">Upgrade</UpgradeButton>` when the action returns an error starting with `Free plan limit`; submit stays disabled until the dialog closes. Homepage `Pricing` split into a server parent reading `auth()` once and a `PricingPlans` client child owning the period toggle: unauthenticated → `/register` link, auth+free → `<UpgradeButton>`, auth+Pro → disabled "Current plan" pill (free card hidden for Pro). Sidebar user menu shows a small shadcn `Badge` reading "PRO" in the dropdown of both collapsed and expanded variants when `session.user.isPro`, plus a small indigo dot on the collapsed avatar trigger. `src/lib/stripe.ts` updated: the `stripe` export is now a `Proxy` that lazily instantiates Stripe on first property access — the `STRIPE_SECRET_KEY` fail-fast still fires, just at first use rather than at module load (Phase 1's import-time throw was killing `next build` once the webhook route module became part of page-data collection and the build env zeroed out the secret). New `src/actions/billing.test.ts` covers auth, invalid interval, missing price config (mutable hoisted `STRIPE_PRICE_IDS`), Stripe rejection, missing email, no-url, success metadata + line items + redirect URLs, plus portal unauthorized / no customer / success / Stripe rejection. `items.test.ts` and `collections.test.ts` mock `@/lib/plan-limits` `canCreate`; new at-limit cases assert the limit-reached error and that `mockCreate` / `mockOwnedCollections` are not invoked. 110 tests pass (was 96), build green (`/api/stripe/webhook` routed as `ƒ`), lint clean for new code (3 unrelated pre-existing warnings remain in `EditCollectionDialog.tsx`, `ItemDrawer.tsx`, `prototypes/homepage/script.js`). End-to-end manual flow still requires the Stripe CLI (`stripe listen --forward-to http://localhost:3000/api/stripe/webhook`) and the Stripe Dashboard prep (test-mode keys, "DevStash Pro" product with $8/mo + $72/yr prices, Customer Portal config). Marketing pricing CTAs while authenticated are reachable only via deep link today — `src/app/page.tsx` still redirects authenticated users to `/dashboard`.
- 2026-05-06: Demo Seed Trim — Trimmed prisma/seed.ts from 5 collections (18 items) to 3 collections (10 items: React Patterns / AI Workflows / Terminal Commands) so the seeded demo user lands at the free-plan ceiling and can exercise the at-limit gating UI.
- 2026-05-06: Upgrade Page + TopBar Upgrade Button — New protected `/upgrade` route (proxy prefix + matcher extended) wraps the existing homepage `PricingPlans` in a focused shell with a "Back to Dashboard" link (mirroring `/settings`); reusing PricingPlans means the monthly/yearly toggle, free vs pro feature lists, "Most popular" badge on Pro, "Current plan" pill for Pro users, and the existing `UpgradeButton` Stripe checkout flow all come for free. CheckoutToast intentionally omitted because Stripe `success_url`/`cancel_url` already point at `/settings`. TopBar gained an `isPro?: boolean` prop and renders a subtle ghost-variant "Upgrade" button (Sparkles icon) for free users only — both as a desktop button (between SearchTrigger and the favorites star) and as the last entry in the mobile overflow `MoreHorizontal` dropdown. Threaded `isPro={session.user.isPro}` through the four layouts that mount TopBar (dashboard / items / collections / favorites). `/items/files` and `/items/images` now `redirect('/upgrade')` at the top of `/items/[type]/page.tsx` (was a 404 since the File/Image item types were dropped on 2026-04-19); the redirect happens for everyone — Pro users land on `/upgrade` and see "Current plan". 110 Vitest tests pass, build green (`/upgrade` registered as `ƒ`), no new lint warnings (the 3 pre-existing ones remain).
- 2026-05-06: AI Integration Research — Documentation-only research output at `docs/ai-integration-plan.md` (driven by `context/research/ai-integration-research.md`). 13-section plan for layering four Pro-only AI features (auto-tag, summary, "explain this code" with streaming, prompt optimizer) onto OpenAI `gpt-5-nano`. Models the foundation files (`src/lib/openai.ts` with the same lazy `Proxy` pattern as `stripe.ts`, `src/lib/ai-limits.ts` mirroring `plan-limits.ts`, server-action-friendly `src/lib/ai-rate-limit.ts`, shared system prompts), four `src/actions/ai.ts` actions (`responses.parse` with `zodTextFormat` for structured output, `responses.create` with `reasoning_effort` knobs per feature), an `/api/ai/explain` POST route streaming `response.output_text.delta` events as `text/plain`, three-layer Pro gating (client UI / server `canUseAi` / JWT `isPro` re-read), Upstash `30/min + 300/24h` per-user limits, monthly soft cap, cost optimization (truncation, caching, `reasoning_effort: 'minimal'`), security (no client keys, prompt-injection envelope, server-side `getItemDetail` ownership re-fetch on every call), Vitest checklist, 7-step implementation order, and an optional v2 `AiCall` Prisma model for per-user metering. No code, no commits, no schema touched.
- 2026-05-06: Language Dropdown — Replaced the free-text Language input in NewItemDialog and ItemDrawer's edit form with a curated Monaco-aware native `<select>`, moved above the Content editor so picking a language switches syntax highlighting in the live `<CodeEditor>` immediately. New `src/lib/code-languages.ts` exports 28 language IDs + display labels (plaintext default, plus bash/c/cpp/csharp/css/dockerfile/go/graphql/html/java/javascript/json/kotlin/lua/markdown/php/powershell/python/ruby/rust/scss/shell/sql/swift/typescript/xml/yaml) and a `getLanguageOptions(currentValue)` helper that prepends an unknown legacy value as a synthetic first option so existing items with custom language strings keep their label on edit. NewItemDialog `emptyForm.language` now defaults to `'plaintext'`; ItemDrawer's `toEditForm` falls back to `'plaintext'` when `item.language` is null. Native `<select>` matches the existing Type dropdown's classes verbatim — no new shadcn primitive. 110 Vitest tests pass, build clean.
- 2026-05-06: AI Auto-Tagging — First Pro-only AI feature, layered on top of the AI integration plan. New `src/lib/openai.ts` (lazy `Proxy` singleton + `AI_MODEL` env-pinned constant defaulting to `'gpt-5-nano'`, mirroring `stripe.ts` so missing keys never break `next build`), new `src/lib/ai-limits.ts` with `canUseAi(userId)` Pro-only gate (re-reads `isPro` from DB), `src/lib/rate-limit.ts` extended with `rateLimiters.ai` (20/h per user) plus a server-action-friendly `checkAiRateLimit` returning `{ ok, retryAfterSeconds }` instead of `NextResponse`. New `src/lib/tags.ts` exposes `parseTagString` / `appendTagToString` (case-insensitive dedupe) shared by both dialogs. New `src/actions/ai.ts` `generateAutoTags({ title, description?, content? })` runs auth → Zod (title required) → `canUseAi` → `checkAiRateLimit` → `openai.responses.create` with `text.format.type: 'json_object'` (per spec gotcha — `zodTextFormat` blows up tokens on `gpt-5-nano`); content truncated to 2000 chars before send, input always includes the literal word "json" (Responses API 400s without it). Manual JSON parse handles both `{tags:[…]}` and bare `[…]` shapes, lowercases, hyphenates whitespace, dedupes, caps at 5. `mapOpenAiError` maps OpenAI 401/429/5xx to user-friendly toast strings. UI: new `SuggestTagsButton` (Sparkles ghost) returns `null` for free users (UI gate; server gate is the source of truth) and renders accepted suggestions as Badge chips with check/X controls — accepted tags appended to the comma-separated tags string, rejected ones disappear. `isPro` threaded through all 4 dashboard-shell layouts (dashboard / items / collections / favorites) → `ItemDrawerProvider` → `ItemDrawer`, and from `TopBar` → `NewItemDialog`. Vitest coverage: 19 cases on `generateAutoTags` (unauth, empty title, not-Pro, rate-limited+retry, success for both JSON shapes, lowercase/hyphenate/dedupe, 5-cap, empty/invalid/missing `output_text`, content truncation, description omit-when-blank, request-shape, json-in-input regression, 401/429/5xx/unknown error mapping) plus 7 cases on the tag helpers. 136 Vitest tests pass (was 110), build green, lint unchanged (3 pre-existing warnings remain). `OPENAI_MODEL=gpt-5-nano` added to `.env.example` (gitignored — not committed). v1 ships auto-tag only; summary / explain / optimize from the AI plan layer on later using the same `openai` + `canUseAi` + `checkAiRateLimit` foundation.
- 2026-05-06: AI Summary Description — Second Pro-only AI feature, layered on the same `openai` + `canUseAi` + `checkAiRateLimit` foundation as auto-tagging. New `generateSummary({ title, type?, content?, url?, language? })` server action in `src/actions/ai.ts` runs auth → Zod (title required, `type` is `enum.optional().catch(undefined)` so unrecognized lowercased names don't reject) → `canUseAi` → `checkAiRateLimit` → `openai.responses.create` with `text.format.type: 'text'` (plain text — no JSON parsing). Content truncated to 2000 chars; input includes only the populated fields (`Type`, `Title`, `Language` when not `plaintext`, `URL`, `Content`) so sparse-input items like links summarize sensibly. `cleanSummary` helper trims, strips wrapping straight/curly/single quotes, collapses whitespace, and caps at 280 chars with `…` ellipsis on overflow. New `SuggestSummaryButton` client component (Sparkles ghost, "Generate description" / "Thinking…", `h-7 px-2 text-xs`) returns `null` for free users; on click it snapshots the current draft via a `getDraft` callback, calls `generateSummary`, replaces the description field on success ("Description generated" toast) and shows `mapOpenAiError` toast on failure. Wired into `NewItemDialog` and `ItemDrawer` edit mode in a flex-justify-between row alongside the "Description" `<Label>` (mirrors the existing description label position; `SuggestTagsButton` lives below the tags input — different convention, intentional since the summary button acts on the field directly below it). `getDraft` honors the type-specific field visibility (only sends `content`/`url`/`language` when their respective inputs are showing). 18 new Vitest cases on `generateSummary` (unauth, empty title, not-Pro, rate-limited+retry, success, quote-stripping, whitespace collapse, 280-char cap with ellipsis, empty/missing `output_text`, content truncation, optional-fields omit-when-blank/include-when-set, request-shape with `text` format + AI_MODEL + instructions assertions, 401/429/5xx/unknown error mapping). 154 Vitest tests pass (was 136), build green, lint unchanged (same 3 pre-existing warnings remain). No new env vars, no schema changes — entire feature is additive on top of the existing AI plumbing.
- 2026-05-06: AI Explain Code — Third Pro-only AI feature, drawer-only, layered on the same `openai` + `canUseAi` + `checkAiRateLimit` foundation. New `explainCode({ itemId })` server action in `src/actions/ai.ts` runs auth → Zod (itemId required) → `canUseAi` → `checkAiRateLimit` → `getItemDetail(itemId, userId)` ownership re-fetch (server-side — client-supplied content is never trusted) → snippet/command type whitelist (`EXPLAINABLE_TYPES`) → empty-content guard → `openai.responses.create` with `text.format.type: 'text'`. Code truncated to 2000 chars; input is `Type | Title | Language? | Code | "Explain what this does."`; instructions ask for a short opener sentence + 3-6 bullets, ~200-300 words, plain Markdown. Output trimmed before return. Errors mapped via the shared `mapOpenAiError`. CodeEditor extended with optional `explain={ itemId, isPro }` prop — when set in `readOnly` mode, the title bar gains an Explain control next to Copy: free users see a `Crown` icon with a Base UI Tooltip ("AI features require Pro subscription"); Pro users see a `Sparkles` button that swaps to `Loader2` while in flight. After a successful call the body swaps to a `markdown-preview` (`react-markdown` + `remark-gfm`) panel and Code/Explain tabs appear in the title bar so users can toggle without re-calling the model. Explanations are ephemeral — kept in component state, regenerated on a fresh open of the drawer. ItemDrawer threads `explain={{ itemId: item.id, isPro }}` only into the read-mode CodeEditor; create/edit-form CodeEditors are untouched. Vitest coverage: 19 new cases on `explainCode` (unauth, empty itemId, not-Pro, rate-limited+retry, foreign-id → "Item not found", non-explainable type rejection, empty-content rejection, ownership re-fetch shape with userId scoping, success, output_text trim, empty/missing output, 2000-char code truncation, Language line omitted when plaintext/null, request-shape with model/format/instructions assertions, 401/429/5xx/unknown error mapping). 173 Vitest tests pass (was 154), build green, lint unchanged (same 3 pre-existing warnings remain). No new env vars, no schema changes — entirely additive on the existing AI plumbing.
- 2026-05-06: AI Prompt Optimizer — Fourth and final Pro-only AI feature, layered on the same `openai` + `canUseAi` + `checkAiRateLimit` foundation. New `optimizePrompt({ itemId })` server action in `src/actions/ai.ts` runs auth → Zod (itemId required) → `canUseAi` → `checkAiRateLimit` → `getItemDetail(itemId, userId)` server-side ownership re-fetch → `OPTIMIZABLE_TYPES` whitelist (`'prompt'` only) → empty-content guard → `openai.responses.create` with `text.format.type: 'text'`. Prompt truncated to 2000 chars; input is `Title | Prompt | "Refine this prompt. Output only the optimized prompt."`; instructions ask the model to rewrite for clarity/specificity/structure, preserve intent, leave already-strong prompts unchanged, and return only the optimized prompt with no preamble/quotes/fences. New `cleanOptimized` helper strips wrapping straight/curly/single quotes and ` ``` ` markdown fences from the output. Returns `{ original, optimized }` so the UI can render a comparison. MarkdownEditor extended with optional `optimize={ itemId, isPro, onAccept }` prop — when set in `readOnly` mode, the title bar gains an Optimize control next to Copy: free users see a `Crown` icon + Base UI tooltip ("AI features require Pro subscription"); Pro users see a `Sparkles` button that swaps to `Loader2` while in flight. On success the body swaps to a 2-column before/after compare panel with `Use original` / `Use optimized` buttons. If the model returns a prompt identical to the input (post-trim), a toast says "Prompt is already well-structured" and the compare panel is skipped. Accept calls `onAccept(optimized)` which the parent (ItemDrawer) implements as `updateItem(...)` with the optimized content + the existing description/url/language/tags/collections preserved; on success the drawer setState updates, toast fires ("Prompt optimized"), `router.refresh()` runs, and the editor dismisses the compare. Reject just clears local state. ItemDrawer threads the optimize prop only when `typeName === 'prompt'`; snippet/command/note read-mode editors are untouched. Vitest coverage: 20 new cases on `optimizePrompt` (unauth, empty itemId, not-Pro, rate-limited+retry, foreign-id → "Item not found", non-prompt type rejection, empty-content rejection, ownership re-fetch shape with userId scoping, success returning original+optimized, output_text trim, straight-quote stripping, code-fence stripping, empty/missing output, 2000-char prompt truncation, request-shape with model/format/instructions assertions, 401/429/5xx/unknown error mapping). 193 Vitest tests pass (was 173), build green, lint unchanged (same 3 pre-existing warnings remain). No new env vars, no schema changes — entirely additive on the existing AI plumbing. Closes the four-feature AI plan from `docs/ai-integration-plan.md`.
- 2026-05-07: UI Polish Pass — Top 5 quick wins from the 2026-05-07 UI review. (1) Sidebar active-link highlighting via `usePathname` on both type and collection links — applies `bg-accent font-medium text-foreground` (or `bg-accent text-foreground` in collapsed mode) to the matching link, with `aria-current="page"` for assistive tech; uses `pathname === href || pathname.startsWith(`${href}/`)` so `/items/snippets` highlights Snippets and `/collections/abc` highlights the matching favorite/recent. (2) GitHub OAuth button on `/register` — copied the divider + `signInWithGitHub` server-action `<form>` block from `/sign-in` so the register page now offers the same OAuth path. (3) Sidebar favorite/recent collection links wired to `/collections/${col.id}` (was `href="#"` no-ops in both modes). (4) Drawer Save/Cancel bar made sticky — restructured the edit branch so `<EditFormFields>` is followed by a `sticky bottom-0 z-10 border-t bg-background/95 backdrop-blur` action bar; previously the bar sat at the top of a long scrollable Sheet and got hidden behind the mobile on-screen keyboard. The read-mode action bar (favorite/pin/copy/edit/delete) keeps its existing top position. (5) Mobile sidebar trigger merged into TopBar — new `src/components/dashboard/MobileSidebarContext.tsx` lifts the Sheet `open` state via React context (`useMobileSidebar` returns a no-op fallback when no provider is mounted, so the component is safe to import outside the dashboard shell), `Sidebar.tsx` switched its mobile Sheet to controlled mode (removed the floating `fixed left-3 top-3` `SheetTrigger`), `TopBar.tsx` renders a `PanelLeft` button at `md:hidden` that calls `setMobileSidebarOpen(true)`, and the four dashboard-shell layouts (dashboard / items / collections / favorites) now wrap with `<MobileSidebarProvider>` between `CommandPaletteProvider` and the layout `<div>`. Settings/profile use a standalone shell so they're untouched. All 193 Vitest tests still pass, build green, lint unchanged (the same 3 pre-existing warnings remain). The remaining 16 issues from the review (Dashboard sidebar link, password strength, ItemCard ARIA, Pagination single-page suppression, CollectionCard link-in-link, breadcrumbs, settings/profile shell, /upgrade Pro card, homepage mobile nav, sort-Select label, ChaosStage aria-hidden, sidebar count contrast, etc.) deferred to follow-up branches.
- 2026-05-07: Actions Refactor — Shared Helpers + Item Schemas — Removed duplication in `src/actions/**` flagged by the refactor-scanner (read-only audit reported "High" density). New `src/types/action.ts` exposes the `ActionResult<T>` discriminated union (was redeclared verbatim in 5 action files). New `src/lib/action-helpers.ts` exposes `requireUserId()` (replaces 14 occurrences of the identical `auth() → !session?.user?.id → return Unauthorized` preamble), `parseOrFail(schema, payload)` (replaces 10 occurrences of the `safeParse → issues[0]?.message → fallback` block, returns `{ ok, data }` / `{ ok, error }`), and `aiGate(userId)` (replaces 4× identical 11-line `canUseAi → PRO_REQUIRED → checkAiRateLimit → retry message` block in ai.ts — also closes the risk of forgetting the rate-limit on a future 5th AI action). New `src/lib/schemas/item.ts` exports `ITEM_TYPES`, an internal `itemBaseFields` const, and the derived `createItemSchema`/`updateItemSchema` (the two schemas overlapped by ~80% in `items.ts`, including a verbatim 17-line URL `.refine()` block); `items.ts` re-exports the payload types so consumer files (`ItemDrawer`, `NewItemDialog`, `ItemCard`) need no edits. `src/lib/plan-limits.ts` gains `checkCreateLimit(userId, name)` returning `{ ok: true } | { ok: false; error }` and centralises the `Free plan limit of <N> <name> reached. Upgrade to Pro for unlimited.` literal that `NewItemDialog`/`NewCollectionDialog` match against via `startsWith('Free plan limit')`. All five action files (`ai`, `billing`, `collections`, `editor-preferences`, `items`) now import the shared helpers and `ActionResult` instead of redeclaring. `items.test.ts` and `collections.test.ts` switched their `vi.mock('@/lib/plan-limits', …)` factory to mock `checkCreateLimit` instead of `canCreate`; the asserted error strings stay identical so no behavioral test rewrites were needed. Net diff inside `src/actions/`: +154 / −348 lines (net −194). Public action signatures, error message strings, and Zod issue messages are unchanged. 193 Vitest tests pass, build green (`/api/stripe/webhook`, `/upgrade` etc. all still routed as `ƒ`), no new lint warnings — the same 3 pre-existing warnings in `EditCollectionDialog`/`ItemDrawer`/`prototypes/homepage/script.js` remain. Deliberately did NOT introduce a `withAuthAction` HOF (call-site type inference is better with the simpler `requireUserId` + `parseOrFail` + `aiGate` trio). Also added `.claude/agents/refactor-scanner.md` — the read-only refactor-scanner subagent that surfaces duplication and proposes extractions per folder type (actions / components / lib / api / hooks / app/route).
- 2026-05-07: Components Refactor Pass — Layered de-duplication of `src/components/**` driven by the refactor-scanner. Quick wins: `NATIVE_SELECT_CLASSES` const for the 3 native `<select>` sites (`NewItemDialog` ×2, `ItemDrawer`); `FreeLimitBanner` (`src/components/billing/`) replaces the verbatim 6-line amber banner in `NewItemDialog` + `NewCollectionDialog`; `useControlledOpen` hook (`src/hooks/`) replaces the 8-line `isControlled / internalOpen / open / setOpen` pattern in both dialogs; `ProBadge` (`src/components/shared/`, `size?: 'sm' \| 'default'` + optional `className`) replaces the 3 inline indigo PRO badges in `Sidebar` (×2) + `SubscriptionCard`. Medium: `EditorActionButton` + `EDITOR_CHROME_BUTTON_CLASS` (`src/components/items/`) consolidates `ExplainButton` (CodeEditor) and `OptimizeButton` (MarkdownEditor) — also de-dupes the 58-char title-bar button class string used 6× across the two files; `useOptimisticToggle<TData>` hook (`src/hooks/`, takes `action + applyOptimistic + rollback + applyResult + optional successMessage`) replaces 4 verbatim optimistic + rollback + `toast.error` + `router.refresh()` pipelines in `ItemCard`, `CollectionDetailActions`, and the `ItemDrawer` favorite/pin toggles (the pin variant uses `successMessage` for "Item pinned"/"Item unpinned"); `DashboardItemRow` (`src/components/items/`, with `trailing` slot + `showPinIcon` flag) replaces the ~70-line bordered-row JSX in `PinnedItems` + `RecentItems` and lets both become server components since the hook + key-handler now live in the row; `makeItemKeyHandler` (`src/lib/item-key-handler.ts`) is the shared `Enter`/`Space` keyboard handler factory consumed by `DashboardItemRow`, `ItemCard`, and `FavoriteItemRow`; `useCollectionDialogs` hook (`src/hooks/use-collection-dialogs.tsx`) returns `{ setEditOpen, setDeleteOpen, dialogs }` so `CollectionCardMenu` and `CollectionDetailActions` no longer mount their own copies of `EditCollectionDialog` + `DeleteCollectionDialog`; `SidebarUserMenuItems` is a file-local component in `Sidebar.tsx` that replaces the 22 verbatim lines of dropdown items previously duplicated between the collapsed and expanded user dropdowns. ItemDrawer breakup (was 686 lines): `useItemDetail(itemId)` hook (`src/hooks/use-item-detail.ts`) owns the `fetch + cancel token + Date re-hydration` for `/api/items/[id]`, returning `{ item, setItem, loading, error }` so the parent can still apply optimistic mutations; `ItemDrawerReadView` (`src/components/items/`) is the read-mode subtree (badges + tags + collections + URL + content with `CodeEditor`/`MarkdownEditor` + dates) accepting `item`, `isPro`, `onAcceptOptimized`; `ItemDeleteDialog` (`src/components/items/`) is the inline delete confirmation. The state-reset on `itemId` change (formerly buried in the same fetch effect) now uses the React-recommended `useState(prev) + if (curr !== prev)` derived-state pattern instead of an effect, so no new `set-state-in-effect` warning is introduced. Larger: `SidebarNavLink` (file-local in `Sidebar.tsx`, takes `href / collapsed / active / icon / label / tooltipLabel? / trailing?`) replaces the doubled collapsed-vs-expanded link rendering across the 3 nav blocks (item types, favorite collections, recent collections), eliminating ~90 lines of duplicated markup. `ItemDrawer.tsx` body went from ~460 to ~280 lines; net diff across the pass: +877 / −761 (sign flips because of the extracted files). Public component prop shapes and behavior unchanged — every callsite still works. 193 Vitest tests pass (no new tests; component-level tests are out of scope per project convention), build green, lint shows the same 3 pre-existing problems (EditCollectionDialog, the original ItemDrawer fetch effect now relocated to use-item-detail.ts, and the homepage prototype). Deferred from the scan: the "compact-mode `ItemCard` rewrite" was flagged as design-call needed; `DashboardItemRow` already covers the actual duplication so the rewrite isn't worth the design churn.
